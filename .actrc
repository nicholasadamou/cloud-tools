# ACT Configuration for cloud-tools project
# This file sets default options for running GitHub Actions locally

# Use linux/amd64 architecture for M-series Macs compatibility
--container-architecture linux/amd64

# Use the medium-sized Ubuntu image (good balance of size and compatibility)
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Enable verbose output for better debugging
# Uncomment the next line if you want verbose output by default
# --verbose

# Bind mount the Docker socket for Docker-in-Docker scenarios
# Uncomment if your workflows need Docker access
# --bind /var/run/docker.sock:/var/run/docker.sock

# Set default environment variables
--env NODE_ENV=test
--env CI=true
